# Versión MPI — Esqueleto

Esta carpeta contiene los archivos necesarios para la futura implementación de la versión paralela del método de Jacobi utilizando MPI.

## Contenido

- `jacobi_mpi.c` — archivo base para la implementación paralela.

## Objetivo

Aquí se desarrollará la versión que distribuye la malla entre varios procesos y realiza comunicación entre fronteras usando MPI.

Aún no está implementado.
